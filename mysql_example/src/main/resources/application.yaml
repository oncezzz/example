server:
  port: 80
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/test?useSSL=false&useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    schema: classpath:schema.sql
    initialization-mode: always